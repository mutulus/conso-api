{% extends 'base.html.twig' %}

{% block title %}Formulaire Personne{% endblock %}

{% block body %}
    <h1 class="mt-5 text-center">Créer une personne</h1>
    {{ form_start(form) }}
    {{ form_row(form.prenom) }}
    {{ form_row(form.nom) }}
    <button type="submit" class="btn btn-primary">Valider</button>
    {{ form_end(form) }}

{% endblock %}
